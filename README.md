# Basic-Calculator
A basic calculator that uses HTML and PHP to perform simple addition, subtraction, multiplication, division, exponential, and root operations.
# How it works
- Uses an HTML form to gather user input
- Sanitizes incoming data to prevent malicious actions such as code injection
- Handles errors before running calculations
- Returns result on the same page
